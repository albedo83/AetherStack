/target/
/.idea/
/.vscode/
.DS_Store

# Acquisition data remains outside the repository. Only small, deliberately
# selected fixtures under tests/fixtures may be versioned.
/data/
/datasets/
/cache/
*.fits
*.fit
*.fts
*.xisf
*.ser

# Local scientific validation corpora and processor exports are private inputs.
# Keep them outside version control even when temporarily copied beside the code.
/ASI294MC-Pro/
*.xpsm
